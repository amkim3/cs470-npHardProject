#ifndef SUBSETSUMBRUTE_H
#define SUBSETSUMBRUTE_H

#include <vector>

using namespace std;

bool subsetSumBrute(vector<long long int> array, int size, long long int sum, vector<long long int> &included, bool vc = false);

#endif